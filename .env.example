# Copy this file to .env and fill in real values.
# Values are referenced from config.json as ${VAR_NAME} and expanded at load time,
# so no token or endpoint ever needs to be committed.

REQUEST_TOKEN=
DIRECTORY_URL=
